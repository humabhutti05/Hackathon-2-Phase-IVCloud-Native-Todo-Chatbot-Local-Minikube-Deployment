apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.db.name }}
spec:
  type: {{ .Values.db.service.type }}
  ports:
    - port: {{ .Values.db.service.port }}
      targetPort: 5432
      protocol: TCP
  selector:
    app: {{ .Values.db.name }}
